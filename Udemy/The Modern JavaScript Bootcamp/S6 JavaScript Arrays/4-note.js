// the for loop in array
const note = ['note1', 'note2', 'note3']
for (let i = 0; i < note.length; i = i + 1) {
    console.log(note[i]) // it will print the note items
}
// the for loop is the same behaviour with the forEach funtion