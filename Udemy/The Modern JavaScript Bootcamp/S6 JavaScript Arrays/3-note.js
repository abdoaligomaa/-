// loop for array using forEech() function 



const note = ['note1', 'note2', 'note3']

note.forEach(function(item) { // this will show the note item 
    console.log(item)
})

// the function can take 2 argument item and index