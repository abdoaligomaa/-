const todos = ['Order cat food', 'Clean kitchen', 'Buy food', 'Do work', 'Exercise']
console.log(`you have ${todos.length}`)
console.log(`todo: ${todos[0]}`) // it will return the first item of the array (Order cat food)
console.log(`todo: ${todos[todos.length-1]}`) // it will return the last item of the array (Exercise)
console.log(`todo: ${todos[todos.length-2]}`) // it will return the second last item of the array (Do work)



// the end of the challenge